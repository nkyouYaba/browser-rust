# browser-rust

## 環境構築
### Rustをインストール
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

### 実行方法
```bash
git clone git@github.com:nkyouYaba/browser-rust.git #最初だけ
chmod +x run_on_wasabi.sh #最初だけ
./run_on_wasabi.sh
```
